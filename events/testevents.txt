add_namespace = test
country_event = {
	id = test.1
	title = test.1.t
	desc = test.1.d
	is_triggered_only = yes
	option = {
		if = {
			limit = {
				FNG = { has_country_flag = second_great_war_winner }
				any_country = {
					is_chinese_country = yes
					has_country_flag = second_great_war_loser
				}
			}
			FNG = { annex_country = { target = PREV transfer_troops = no } }
		}
		name = test.1.a
	}
}